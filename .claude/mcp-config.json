{
  "mcpServers": {
    "latanda-postgresql": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://latanda_user:latanda123@localhost:5432/latanda_production"
      ],
      "description": "PostgreSQL database connector for La Tanda production database",
      "env": {
        "PGPASSWORD": "latanda123"
      }
    }
  }
}
